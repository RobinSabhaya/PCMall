<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PCMall - search product</title>
</head>

<body>
    <% if(productData){%>
        <div class="product_bx">
            <a href="/singleproduct/<%= productData._id %>" class="flex justify-center items-center flex-col">
                <div>
                    <img src="<%=BASE_URL %>/uploads/<%= productData.img[0]%>" alt="img" height="120px" width="120px" />
                </div>
                <div>
                    <%= productData.name%>
                </div>
                <div class="text-red-700 text-xl font-bold m-2">₹<%= productData.price%>
                </div>
            </a>
            <div class="flex gap-5">
                <button class="bg-red-700 text-white text-sm px-5 py-3 rounded-full mt-2 btn"
                    data-order="<%= JSON.stringify(productData) %>">Add to Cart</button>
                <div><i class="lar la-heart product_like" data-wishproduct="<%= JSON.stringify(productData) %>"></i>
                </div>
            </div>
        </div>
        <%}%>

</body>

</html>